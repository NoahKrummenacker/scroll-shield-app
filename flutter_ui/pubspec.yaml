name: scroll_shield
description: ScrollShield - Block Instagram Reels and YouTube Shorts.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.0
  shared_preferences: ^2.3.0

flutter:
  uses-material-design: true
